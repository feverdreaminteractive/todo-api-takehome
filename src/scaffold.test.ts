// Placeholder test for the scaffolding step (issue #1) -- proves Jest/ts-jest are wired up
// correctly. Replaced by real coverage as each layer lands in subsequent issues.
describe('scaffold', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
